{% extends "base.html.twig" %}

{% block body %}

<div class="container">
    <h1 class="display-1 mt-4">PetShop</h1>

    <div class="row">
        <div class="col-md-6">
            <table class="table table-striped">
                <tr>
                    <th colspan="2">Quantidade de animais por cliente</th>
                </tr>
                {% for qtd in qtd_animais %}
        
                    <tr>
                        <th>{{ qtd.nome }}</th>
                        <td>{{ qtd.qtd }}</td>
                    </tr>
                    
                {% endfor %}
                
            </table>
        </div>
        <div class="col-md-6">
            <table class="table table-striped">
                <tr>
                    <th colspan="2">Quantidade de raças por animal</th>
                </tr>
                {% for qte in qtd_racas %}
        
                    <tr>
                        <th>{{ qte.nome }}</th>
                        <td>{{ qte.qtd }}</td>
                    </tr>
                    
                {% endfor %}
                
            </table>
        </div>
    </div>
    
</div>

{% endblock %}